# Unreal Engine generated folders — never commit these.
# (**/ prefix so they match inside GAM303_FPSPuzzle/ too, since the
# .uproject lives in a subfolder of the repo)
**/Saved/
**/Intermediate/
**/DerivedDataCache/
**/Binaries/
**/Build/
**/Platforms/

# Built data for maps
*_BuiltData.uasset

# Visual Studio / Rider / VS Code
.vs/
.idea/
*.sln
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.opendb

# Compiled files (not used in a Blueprint-only project, kept for safety)
*.obj
*.pch
*.dll
*.lib
*.exe
*.app
*.ipa

# OS cruft
Thumbs.db
.DS_Store
